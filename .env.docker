# Docker Compose Environment Variables
# These are used by docker-compose.yml to configure the container

# Host User/Group IDs for volume permissions
# Run 'id -u' and 'id -g' on your host to get these values
HOST_UID=1000
HOST_GID=1000

# Application Environment
APP_ENV=local